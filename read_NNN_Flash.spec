# -*- mode: python -*-

block_cipher = None


a = Analysis(['read_NNN_Flash.py'],
             pathex=['C:\\Users\\Xabier\\Desktop\\Td5_inside_20161221\\Ekaitza_Itzali'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='read_NNN_Flash',
          debug=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='read_NNN_Flash')
